# Actualización de landings Kavana (2026-08-04)

> **Para Hermes:** ejecutar este plan por fases, verificando cada fase antes de pasar a la siguiente.

**Goal:** Alinear las landings de kavana-landing con el estado real de los repos tras el trabajo del 2026-08-04 (asistente RAG en Warehouse, BusRoad nuevo, números reales de tests/endpoints/ADRs).

**Architecture:** Repos estáticos HTML en `kavana-landing` (Vercel). Cada producto tiene su `index.html` + la principal `index.html` lista los 4 productos. Solo tocar HTML/SEO, sin cambios de infraestructura.

**Tech Stack:** HTML/CSS vanilla, patrón portfolio existente (secciones `st`, `h3` con capacidades verificables).

---

## Contexto actual (datos verificados contra el código hoy)

| Producto | Landing dice | Realidad (código) |
|---|---|---|
| Warehouse | 44 endpoints, 38 tests, 4 ADRs, sin asistente | **45 endpoints, 45+ tests (60 en suite), 5 ADRs, asistente RAG + blindaje demo** |
| RouteAI | 25 endpoints, 50 tests, 7 ADRs | ✅ Coherente (25 endpoints reales, 48-50 tests) |
| Manufacturing | 233 tests, 5 ADRs | ✅ Coherente (208+ tests backend, ADRs) |
| Calculadora | 34 tests, 9 ADRs | ✅ Coherente |
| Principal | Lista 4 productos | **Falta BusRoad** (proyecto nuevo de hoy) |
| BusRoad | No existe landing | **Nuevo: Vue 3 + FastAPI + Kubernetes + ORS (restricciones reales)** |

---

## FASE 1: Warehouse — corregir números y añadir capacidades nuevas

**Objetivo:** `warehouse/index.html` refleja el estado real (asistente RAG + blindaje demo + 45 endpoints + 5 ADRs).

**Archivo:** `warehouse/index.html`

**Tareas:**
1. `44 endpoints` → `45 endpoints` (si aparece como número literal; verificar contexto: "44 endpoints" en sección de capacidades)
2. `38 tests de API + 3 de frontend` → `45 tests de API + 3 de frontend` (README actualizado hoy dice 45)
3. `4 ADRs` → `5 ADRs`
4. Añadir a la sección de capacidades técnicas:
   - `<h3>Asistente técnico RAG</h3>` — responde sobre la documentación real del proyecto (readme, ADRs, código) con modelo DeepSeek
   - `<h3>Blindaje de demo</h3>` — modo solo lectura para supervisores de visita con caducidad 24h (patrón RouteAI)
5. Verificar que la sección "capacidades" mantiene el formato de las demás (h3 + párrafo breve)

**Verificación:** `grep -c "Asistente técnico\|asistente RAG" warehouse/index.html` → ≥1; `grep -o "45 endpoints" warehouse/index.html` → 1; `grep -o "5 ADR" warehouse/index.html` → 1

---

## FASE 2: Principal — añadir BusRoad al portfolio

**Objetivo:** la principal (`index.html`) muestra 5 productos, incluyendo BusRoad.

**Archivo:** `index.html`

**Tareas:**
1. Localizar la sección de productos/portfolio (donde se listan routeai, warehouse, manufacturing, calculadora)
2. Añadir tarjeta/entrada de **Kavana BusRoad** con:
   - Título: BusRoad
   - Descripción: rutas para vehículos grandes (autobuses, camiones) con restricciones de altura, anchura, largo y peso reales en Europa
   - Tecnologías: Vue 3 · FastAPI · Kubernetes · OpenRouteService
   - Enlace: la app desplegada `https://frontend-alpha-eight-85.vercel.app` (o `busroad.kavanasystems.com` cuando exista)
3. Mantener el patrón visual de las tarjetas existentes (mismo estilo/clases)

**Verificación:** `grep -c "BusRoad" index.html` → ≥1

---

## FASE 3: BusRoad — crear landing propia (opcional pero recomendado)

**Objetivo:** landing individual de BusRoad con el patrón portfolio (problema-primero, métricas verificables, sección Flujo).

**Archivo nuevo:** `busroad/index.html`

**Tareas:**
1. Crear directorio `busroad/` y `busroad/index.html` copiando la estructura de `warehouse/index.html` como plantilla
2. Contenido:
   - **Por qué lo construí:** el hermano de Jorge es conductor de autobús; las rutas de coche no valen para vehículos grandes (puentes bajos, calles estrechas)
   - **Capacidades técnicas verificables:**
     - `<h3>API REST con FastAPI</h3>` — geocodificación + cálculo de rutas con restricciones
     - `<h3>OpenRouteService (driving-hgv)</h3>` — restricciones reales de altura/anchura/largo/peso en Europa (datos OpenStreetMap)
     - `<h3>Kubernetes (k3s)</h3>` — backend desplegado en clúster con TLS Let's Encrypt
     - `<h3>Frontend PWA con Vue 3</h3>` — compara ruta segura vs convencional
     - `<h3>Navegación con waypoints</h3>` — abre Google Maps/Waze forzando la ruta segura
   - **Flujo:** 1) escribes origen/destino con dirección completa, 2) pones las dimensiones del vehículo, 3) obtienes ruta segura + convencional, 4) navegas con waypoints
   - **Métricas reales:** verificar número de endpoints del backend (grep en `motor.py`) y tests si existen
3. Añadir al sitemap.xml
4. Enlazar desde la principal (ya en Fase 2)

**Verificación:** `curl -s localhost...` no aplica; comprobar que el HTML valida (mismas clases), `grep -c "driving-hgv" busroad/index.html` → ≥1

---

## FASE 4: Verificación global + deploy

**Objetivo:** todo desplegado y verificado en producción.

**Tareas:**
1. `git add -A && git commit` en kavana-landing con mensaje descriptivo por fases
2. `git push origin main` (dispara deploy de Vercel)
3. Verificar en producción:
   - `curl -s https://www.kavanasystems.com/warehouse/ | grep -c "asistente"` → ≥1
   - `curl -s https://www.kavanasystems.com/ | grep -c "BusRoad"` → ≥1
   - `curl -s https://www.kavanasystems.com/ | grep -c "busroad"` → ≥1 (sitemap/links)
   - HTTP 200 en todas las páginas
4. Verificar sitemap.xml incluye `/busroad/`

**Verificación final:** todas las URLs del sitemap responden 200

---

## Riesgos y notas

- **No inventar números**: cada cifra puesta en una landing debe estar verificada contra el código (ya hecho en el contexto).
- **No romper el patrón visual**: copiar clases CSS existentes, no introducir estilos nuevos.
- **URL de BusRoad**: usar la URL real de Vercel (`frontend-alpha-eight-85.vercel.app`) hasta que haya dominio propio; si el usuario añade el CNAME más adelante, actualizar.
- **Sitemap**: no olvidar añadir busroad, o Google no la indexará (Jorge revisa indexación).
- **UX/content consulta**: las landings son contenido de Jorge; al terminar, mostrarle el resultado y ofrecer ajustes de redacción (sin em-dashes, tono humano).
